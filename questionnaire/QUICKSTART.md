# 快速启动指南

## 前置要求

确保已安装以下软件：
- **JDK 17** 或更高版本
- **Maven** （后端已包含Maven Wrapper）
- **MySQL 8.0** 或更高版本
- **Node.js 20.19** 或更高版本
- **npm** 或 **yarn**

## 三步启动

### 第一步：初始化数据库

```bash
# 1. 登录MySQL
mysql -u root -p

# 2. 创建数据库
CREATE DATABASE questionnaire_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 3. 退出MySQL
exit

# 4. 导入表结构
mysql -u root -p questionnaire_db < database/schema.sql
```

**Windows用户提示：** 确保MySQL的bin目录在PATH环境变量中。

### 第二步：修改数据库配置

编辑 `backend/src/main/resources/application.properties`：

```properties
# 修改这三行为你的实际配置
spring.datasource.url=jdbc:mysql://localhost:3306/questionnaire_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=你的MySQL密码
```

### 第三步：启动项目

**方式一：使用命令行（推荐）**

打开两个终端窗口：

```bash
# 终端1 - 启动后端
cd backend
./mvnw spring-boot:run
# Windows用户使用：mvnw.cmd spring-boot:run

# 终端2 - 启动前端
cd frontend
npm install
npm run dev
```

**方式二：使用IDE**

1. 后端：用IDEA或Eclipse打开backend目录，运行BackendApplication.java
2. 前端：在frontend目录执行 `npm install && npm run dev`

## 访问系统

启动成功后，在浏览器打开：

**前端地址：** http://localhost:5173  
**后端地址：** http://localhost:8080

## 测试流程

### 1. 创建问卷

1. 访问首页 http://localhost:5173
2. 点击"创建新问卷"
3. 填写问卷信息：
   - 标题：例如"满意度调查"
   - 说明：例如"请您如实填写"
   - 类型：选择"匿名问卷"
4. 添加题目：
   - 点击"添加题目"
   - 填写题目标题
   - 选择题目类型（单选/多选/填空/评分）
   - 如果是单选或多选，添加选项
   - 勾选"是否必答"
5. 点击"保存问卷"

### 2. 发布问卷

1. 进入"问卷管理"页面
2. 找到刚创建的问卷
3. 点击"发布"按钮
4. 记下该问卷的"访问码"（8位字符）

### 3. 填写问卷

1. 返回首页
2. 在"访问问卷"区域输入访问码
3. 点击"开始填写"
4. 填写所有题目
5. 点击"提交问卷"

### 4. 查看统计

1. 进入"问卷管理"页面
2. 点击对应问卷的"统计"按钮
3. 查看统计结果和图表
4. 可点击"导出数据"下载统计报告

## 常见问题

### 问题1：后端启动失败 - 数据库连接错误

**原因：** 数据库配置不正确或MySQL未启动

**解决：**
1. 确认MySQL服务已启动
2. 检查 `application.properties` 中的数据库配置
3. 确认数据库已创建且表结构已导入

### 问题2：前端启动失败 - 端口被占用

**原因：** 5173端口被其他程序占用

**解决：**
```bash
# 修改 frontend/vite.config.js，添加：
export default defineConfig({
  server: {
    port: 3000  // 改用其他端口
  }
})
```

### 问题3：前端无法访问后端API

**原因：** CORS跨域问题或后端未启动

**解决：**
1. 确认后端已启动（访问 http://localhost:8080）
2. 检查浏览器控制台是否有CORS错误
3. 确认后端CorsConfig配置正确

### 问题4：Maven依赖下载慢

**解决：** 配置国内镜像

在 `backend/pom.xml` 中添加：
```xml
<repositories>
    <repository>
        <id>aliyun</id>
        <url>https://maven.aliyun.com/repository/public</url>
    </repository>
</repositories>
```

### 问题5：npm安装依赖慢

**解决：** 使用国内镜像

```bash
# 使用淘宝镜像
npm config set registry https://registry.npmmirror.com

# 或使用cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

## 默认测试账号

数据库初始化时会创建两个测试用户：

- 管理员：`admin` / `password`
- 普通用户：`user1` / `password`

**注意：** 当前版本未实现登录功能，创建问卷时默认使用ID为1的用户。

## 开发模式

### 后端热重载

Spring Boot DevTools已包含在依赖中，修改Java代码后会自动重启。

### 前端热重载

Vite自动支持热重载，修改Vue文件后浏览器会自动刷新。

## 生产构建

### 构建后端

```bash
cd backend
./mvnw clean package
# 生成的jar文件在 target/backend-0.0.1-SNAPSHOT.jar
```

### 构建前端

```bash
cd frontend
npm run build
# 生成的静态文件在 dist/ 目录
```

## 端口说明

- **后端：** 8080
- **前端开发服务器：** 5173
- **MySQL：** 3306

## 下一步

1. 尝试创建不同类型的问卷
2. 测试必答题验证
3. 查看统计图表效果
4. 导出统计数据
5. 根据需求修改复古风格样式

## 技术支持

遇到问题？

1. 查看控制台错误信息
2. 检查浏览器开发者工具的Network标签
3. 查看后端日志输出
4. 阅读 README.md 获取更多信息

祝使用愉快！ 🎉

